#include "push_swap.h"

void    init_stack(t_stack *stack)
{
    stack->value = 0;
    stack->index = 0;
    stack->pos = 0;
    stack->prev = NULL;
    stack->index = NULL;
}

void push_swap()
{
    t_stack *stack_a;

    init_stack(stack_a);

    while (stack_a)
    {
        
    }

}